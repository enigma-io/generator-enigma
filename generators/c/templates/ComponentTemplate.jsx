'use strict';

var React = require('react/addons');
var <%= componentName %> = React.createClass({
    render: function() {
        return (
            <div />
        );
    }
});

module.exports = <%= componentName %>;
